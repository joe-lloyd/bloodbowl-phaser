- in the kick off table when dodgy snack is applied it shows the correct stats updated but it reduced like this (MA + Sprint) - 1, instead of (MA - 1) + sprint. ie the player can still move their full unmodified MA score but sprint only 1 square, but they should only be able to move MA - 1 and then sprint twice (or 3 times if they have the special skill)

- stab still appears along with other special attacks when you move next to another player when it should not be there because we added anew blitz with special move button that should be used for moveing and special attacks. additionally when i declaired a blits with a special attacki and then move and click on an oppoosing player team mate it should use the special attackl instead of the regular block.

- handoff is treated like a pass but its different in the rules, they are as follows 
    PERFORMINGA HAND-OFF
    During each Turn, a single player on the active team may declare a Hand-off Action in order to attempt to give the ball to another player. When a player declares a Hand-off Action they are first allowed to make a Move Action, though they cannot continue to move after the Hand-off Action has been attempted. A player does not have to be in possession of the ball to declare a Hand-off Action, and may attempt to pick up the ball as part of their Move Action. To perform a Hand-off action, the player that declared the Hand-off must finish their Move Action adjacent to a Standing team-mate who has not lost their Tackle Zone. The team-mate must then attempt to Catch the ball as

- when it comes to actions lets let players change their minds if they didnt roll any dice yet. i keep getting locked out of blitz because i click blitz and then change my mind and wanna activate someone else instead but then i cant blitz because the decleration ahppens way too early and clocks me out. this should really only happppen with bone head or other big guys that roll as soon as their action is declaired, then they must do as the action says.

- when hovering the sideline crew symbols put a tool tip in the top right just like when hovering a player that explains everrythig as to what it is

- implement the distacted status, really stupid or bone ehad still shows the tackle szone even when they failed the roll but they should be set to distracted loosing their tacklezone until the have a successful action.

- FIXED 2026-07-27 (fix-drive-transition-lifecycle): scoring a touchdown logged "[Orchestrator] No handler for phase: TOUCHDOWN", KO recovery silently produced a player who existed twice at once (one copy on the pitch, one stuck in the KO box), and setup then refused to complete because the team could not field seven. TOUCHDOWN now has a real phase handler that owns the celebration window; KO recovery is rolled one player at a time through a single `movePlayerToBox` seam (src/game/rules/playerLocation.ts) so a player is one record in one place; setup completes at `min(7, availablePlayers)`. Covered by __tests__/headless/driveTransition.test.ts and __tests__/unit/controllers/TouchdownPhaseHandler.test.ts.

- FIXED 2026-07-27 (fix-drive-transition-lifecycle): starting a second match in the same browser session crashed at the kickoff with "Cannot read properties of null (reading 'queueDepthSort')" from `new BallSprite`. Cause was listener lifetime, not the sprite: the first match's KickoffPhaseHandler stayed subscribed to the shared EventBus and captured a destroyed scene. GameScene.shutdown now destroys the orchestrator (which exits the handler and unsubscribes it), every scene EventBus subscription is tracked and removed, ServiceContainer.reset() runs before a new local scene is built, and the sprite factories no-op with a warning on an inactive scene. Covered by __tests__/unit/controllers/sceneLifecycle.test.ts.