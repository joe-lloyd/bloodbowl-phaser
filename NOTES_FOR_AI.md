- FIXED 2026-07-27 (fix-drive-transition-lifecycle): scoring a touchdown logged "[Orchestrator] No handler for phase: TOUCHDOWN", KO recovery silently produced a player who existed twice at once (one copy on the pitch, one stuck in the KO box), and setup then refused to complete because the team could not field seven. TOUCHDOWN now has a real phase handler that owns the celebration window; KO recovery is rolled one player at a time through a single `movePlayerToBox` seam (src/game/rules/playerLocation.ts) so a player is one record in one place; setup completes at `min(7, availablePlayers)`. Covered by __tests__/headless/driveTransition.test.ts and __tests__/unit/controllers/TouchdownPhaseHandler.test.ts.

- FIXED 2026-07-27 (fix-drive-transition-lifecycle): starting a second match in the same browser session crashed at the kickoff with "Cannot read properties of null (reading 'queueDepthSort')" from `new BallSprite`. Cause was listener lifetime, not the sprite: the first match's KickoffPhaseHandler stayed subscribed to the shared EventBus and captured a destroyed scene. GameScene.shutdown now destroys the orchestrator (which exits the handler and unsubscribes it), every scene EventBus subscription is tracked and removed, ServiceContainer.reset() runs before a new local scene is built, and the sprite factories no-op with a warning on an inactive scene. Covered by __tests__/unit/controllers/sceneLifecycle.test.ts.
