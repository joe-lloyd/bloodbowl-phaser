import { SkillRule } from "../SkillRule";

/**
 * Secret Weapon resolves during ClearPitchOperation at the end of a Drive.
 * The rule object registers the trait as implemented; the operation owns the
 * drive-wide participation/status transition.
 */
export const SecretWeaponRule: SkillRule = {};
