import { SkillRule } from "../SkillRule";

/**
 * Multiple Block is orchestrated by BlockManager because it is a compound,
 * two-target Block Action rather than a modifier on one block roll.
 */
export const MultipleBlockRule: SkillRule = {};
